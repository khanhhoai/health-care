<div class="container">
    <div class="mt-4">
        <h2>Update article</h2>
        <form method="POST" action="/hospital/{{hospitals._id}}?_method=PUT">
            <div class="form-group">
                <label for="title">Name</label>
                <input type="text" class="form-control" value="{{hospitals.title}}" id="title" name="title" >
            </div>

            {{!-- <div class="form-group">
                <label for="description">Description</label>
                <textarea class="form-control" id="description" name="description">{{news.description}}</textarea>
            </div> --}}

            <div class="form-group">
                <label for="image">Image</label>
                <input type="text" class="form-control" value="{{hospitals.image}}"  id="image" name="image">
            
            </div>
        
            <button type="submit" class="btn">Update</button>
        </form>
    </div>
</div>